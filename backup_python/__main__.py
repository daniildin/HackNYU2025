"""
This project has been converted to Java (Gradle). Please run the Java application.

To build and run:
  ./gradlew run     (on mac/linux)
  gradlew.bat run   (on Windows)

The Java entrypoint is: com.nova.App
"""

import sys
print("Project converted to Java. See README.md and run ./gradlew run")
sys.exit(0)